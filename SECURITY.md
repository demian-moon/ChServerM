# 보안 정책 (Security Policy)

이 문서가 **지원 버전과 취약점 신고 절차의 정본**이다.
버전 번호의 의미는 [docs/VERSIONING.md](docs/VERSIONING.md), 결정 근거는
ADR-0072([docs/DECISIONS.md](docs/DECISIONS.md))에 있다.

## 지원 버전

전 패키지가 락스텝 단일 버전(ADR-0069)이므로 지원 단위는 패키지가 아니라
**프레임워크 릴리스 전체**다.

### 1.0 이후 (정식 정책)

| 버전 | 지원 기간 | 받는 것 |
|---|---|---|
| **최신 minor** (`N.M`) | 다음 minor 출시까지 | 기능 · 버그 수정 · 보안 패치 전부 |
| **직전 minor** (`N.M-1`) | 다음 minor 출시 후 **6개월** | **보안 패치만** (patch 릴리스로 백포트) |
| 그 이전 minor | — | 지원 없음 — 최신으로 업그레이드 안내 |
| major 전환기 | 새 major 출시 후 **6개월** | 구 major 의 마지막 minor 가 보안 패치만 수령 |

업그레이드 비용은 정책의 일부다: minor 는 비파괴(1.0 이후, VERSIONING.md 판정표
기준)이므로 "최신 minor 로 올리라"는 안내가 소비자에게 파괴적 작업을 요구하지 않는다.

### 0.x 동안 (현재)

**최신 릴리스만 지원한다.** 0.x 는 공개 API 를 안정으로 간주하지 않으며(ADR-0069),
보안 수정도 최신 버전에만 실린다. 이 정책의 정식 효력은 1.0 선언과 함께 시작된다.

## 취약점 신고

**공개 이슈로 취약점을 올리지 말 것.** 수정이 나오기 전의 공개는 모든 사용자를
위험에 노출시킨다.

- **채널**: GitHub **Private Vulnerability Reporting** (저장소 Security 탭 →
  "Report a vulnerability"). ⚠ 이 기능은 저장소 공개 전환과 함께 활성화된다 —
  공개 전환 체크리스트에 포함되어 있다.
- **절차**: 접수 → 재현·심각도 판정 → 지원 중인 모든 버전에 수정 백포트 →
  patch 릴리스 + 보안 권고(advisory) 공개. 신고자가 원하면 권고에 기여자로 명기한다.
- **조정 공개(coordinated disclosure)**: 신고 후 **90일** 또는 수정 릴리스 공개 중
  이른 쪽까지 비공개 유지를 요청한다.

## 신고 범위 (무엇이 취약점인가)

프레임워크는 보안 축을 **조립으로 선택**한다. 따라서 다음은 취약점이 아니라
문서화된 계약이다:

- 보안 축을 "없음"으로 조립한 구성의 무보호 통신 — 계약에 명시되어 있다(ADR-0017)
- 문서화된 상한·옵션을 끄거나 느슨하게 조립해서 생기는 자원 고갈
- `Samples/`·`Templates/`·`Bench/` — 배포물이 아니다(VERSIONING.md 비계약 목록)

반면 **기본값 구성에서 뚫리는 것, 문서화된 보안 계약(THREAT-MODEL.md 의 완화책)이
실제로 동작하지 않는 것**은 심각도 최상으로 다룬다 — "완화책이 무동작인데 아무도
모르는 상태"가 이 프로젝트가 레거시에서 배운 최악의 실패 형태다.
