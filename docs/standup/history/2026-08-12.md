# 2026-08-12 — 메타 패키지 + Phase 22 사전 점검(AOT 4종·컨테이너·K8s)

## 한 일

- **메타 패키지 `ChServerM` 구성** (ADR-0070, 코드 미커밋) — `Server/ChServerM` 신설.
  코드 없는 의존성 전용 패키지, realtime-stateful 최소 조합 8개(Hosting·Framing·
  Concurrency·Transport.Tcp·Transport.InMemory·Serialization.MemoryPack·SourceGen·
  Analyzers) = 시작 가이드/EchoServer 집합. 벤더 어댑터·선택 축 제외.
  - 검증: 솔루션 pack 33개 성공 → 로컬 피드+격리 캐시에서 **메타 하나만 참조한
    프로젝트가 조립·기동·에코 왕복 + `[MessageHandler]` 생성 코드 경로까지 동작**
    (전이 의존성으로 온 SourceGen 이 실제로 등록 코드를 생성함을 실행으로 확인).
    전체 솔루션 `-warnaserror` 빌드 통과
  - 함정 발견: pack 이 MemoryPack 어댑터의 전이 PackageReference 를 메타의
    **직접 의존성**으로 올린다(nuspec 실측) → `PrivateAssets="all"` 로 차단,
    최종 nuspec 의존성은 의도한 8개 정확히
- **Native AOT 샘플 전체 검증** (Phase 22 항목 완료) — GameRoom·Matchmaking·
  StatelessWeb csproj 에 `PublishAot` 선언 추가(EchoServer 는 기존). eng/build.ps1
  AOT 게이트가 선언을 자동 발견하므로 4종 전부 CI 게이트 편입. 4종 전부
  publish(경고=오류) + 실행 자체 검증 통과 — StatelessWeb(Kestrel h2c + Protobuf)도
  첫 시도 통과
- **EchoServer SIGTERM 정상 종료** — `--serve` 가 SIGINT(CancelKeyPress)만 받아
  컨테이너/K8s 종료(SIGTERM)에서 드레인 없이 즉사하는 실결함 →
  `PosixSignalRegistration` 등록. `docker stop` 에서 드레인 로그 + exit 0 실증
- **컨테이너 이미지 + K8s 매니페스트** (Phase 22 항목 진행) — `deploy/echo-server/`
  Dockerfile(SDK 태그 10.0.201 = global.json 피처 밴드 고정, AOT + runtime-deps +
  비루트, 콘텐츠 ~51MB) + 루트 `.dockerignore` + K8s Deployment/Service.
  - 실증: 이미지 빌드(리눅스 AOT 링크) → 기동 → **외부 TCP 클라이언트(메타 패키지
    소비)로 200회 왕복** → SIGTERM 드레인 exit 0
  - 매니페스트는 kubeconform `-strict` 통과. 실클러스터 apply 는 로컬 클러스터
    부재로 미실증(잔여)

## 결정

- **ADR-0070** — 메타 패키지는 `ChServerM` 하나, 내용은 realtime-stateful 최소 조합.
  전부 담는 `.All`(벤더 격리 붕괴)·프로필별 메타 2개(stateless-web 은 세션 저장소
  벤더 선택을 메타가 대신 못 함) 탈락. 메타 목록 변경 = 제품 변경(추가 minor, 제거 major)

## 측정

- 없음 (성능 무관 세션. 이미지 콘텐츠 ~51MB 는 기록 목적)

## 막힌 것 / 미완료

- **⚠ 이 세션의 코드 산출물 전부 미커밋** — 커밋은 사용자 확인 후. 제안 분할:
  feat(packaging) 메타 패키지(+ADR)는 문서 커밋에 ADR 포함됨 / build(samples) AOT
  전체 검증 + SIGTERM / build(deploy) 컨테이너·K8s 예제
- K8s 실클러스터 apply·rollout 미실증 — 로컬 클러스터 부재(docker-desktop K8s 비활성)
- 원격 CI 미실행 — AOT 4종 확대·메타 패키지 포함 매트릭스는 다음 푸시가 확증
- 사용자 결정 3건 여전히 대기: 라이선스(발행 선결) · 지원 정책 · 패키지 서명

## 다음

1. **미커밋 코드 커밋**(사용자 확인) 후 푸시 → 원격 CI 로 AOT 4종 매트릭스 확증
2. 사용자 결정 3건(라이선스·지원 정책·서명) — 풀리면 발행 준비
3. Phase 22 잔여 — K8s 실클러스터 검증(클러스터 확보 시) · 전 Phase 게이트 재확인 ·
   문서 전체 검토(DocFX 경고 58건 포함)
