# 2026-08-12 — 메타 패키지 + Phase 22 사전 점검(AOT 4종·컨테이너·K8s)

## 한 일

- **메타 패키지 `ChServerM` 구성** (ADR-0070, 코드 미커밋) — `Server/ChServerM` 신설.
  코드 없는 의존성 전용 패키지, realtime-stateful 최소 조합 8개(Hosting·Framing·
  Concurrency·Transport.Tcp·Transport.InMemory·Serialization.MemoryPack·SourceGen·
  Analyzers) = 시작 가이드/EchoServer 집합. 벤더 어댑터·선택 축 제외.
  - 검증: 솔루션 pack 33개 성공 → 로컬 피드+격리 캐시에서 **메타 하나만 참조한
    프로젝트가 조립·기동·에코 왕복 + `[MessageHandler]` 생성 코드 경로까지 동작**
    (전이 의존성으로 온 SourceGen 이 실제로 등록 코드를 생성함을 실행으로 확인).
    전체 솔루션 `-warnaserror` 빌드 통과
  - 함정 발견: pack 이 MemoryPack 어댑터의 전이 PackageReference 를 메타의
    **직접 의존성**으로 올린다(nuspec 실측) → `PrivateAssets="all"` 로 차단,
    최종 nuspec 의존성은 의도한 8개 정확히
- **Native AOT 샘플 전체 검증** (Phase 22 항목 완료) — GameRoom·Matchmaking·
  StatelessWeb csproj 에 `PublishAot` 선언 추가(EchoServer 는 기존). eng/build.ps1
  AOT 게이트가 선언을 자동 발견하므로 4종 전부 CI 게이트 편입. 4종 전부
  publish(경고=오류) + 실행 자체 검증 통과 — StatelessWeb(Kestrel h2c + Protobuf)도
  첫 시도 통과
- **EchoServer SIGTERM 정상 종료** — `--serve` 가 SIGINT(CancelKeyPress)만 받아
  컨테이너/K8s 종료(SIGTERM)에서 드레인 없이 즉사하는 실결함 →
  `PosixSignalRegistration` 등록. `docker stop` 에서 드레인 로그 + exit 0 실증
- **컨테이너 이미지 + K8s 매니페스트** (Phase 22 항목 진행) — `deploy/echo-server/`
  Dockerfile(SDK 태그 10.0.201 = global.json 피처 밴드 고정, AOT + runtime-deps +
  비루트, 콘텐츠 ~51MB) + 루트 `.dockerignore` + K8s Deployment/Service.
  - 실증: 이미지 빌드(리눅스 AOT 링크) → 기동 → **외부 TCP 클라이언트(메타 패키지
    소비)로 200회 왕복** → SIGTERM 드레인 exit 0
  - 매니페스트는 kubeconform `-strict` 통과. 실클러스터 apply 는 로컬 클러스터
    부재로 미실증(잔여)

## 결정

- **ADR-0070** — 메타 패키지는 `ChServerM` 하나, 내용은 realtime-stateful 최소 조합.
  전부 담는 `.All`(벤더 격리 붕괴)·프로필별 메타 2개(stateless-web 은 세션 저장소
  벤더 선택을 메타가 대신 못 함) 탈락. 메타 목록 변경 = 제품 변경(추가 minor, 제거 major)

## 측정

- 없음 (성능 무관 세션. 이미지 콘텐츠 ~51MB 는 기록 목적)

## 막힌 것 / 미완료

- **⚠ 이 세션의 코드 산출물 전부 미커밋** — 커밋은 사용자 확인 후. 제안 분할:
  feat(packaging) 메타 패키지(+ADR)는 문서 커밋에 ADR 포함됨 / build(samples) AOT
  전체 검증 + SIGTERM / build(deploy) 컨테이너·K8s 예제
- K8s 실클러스터 apply·rollout 미실증 — 로컬 클러스터 부재(docker-desktop K8s 비활성)
- 원격 CI 미실행 — AOT 4종 확대·메타 패키지 포함 매트릭스는 다음 푸시가 확증
- 사용자 결정 3건 여전히 대기: 라이선스(발행 선결) · 지원 정책 · 패키지 서명

## 다음

1. **미커밋 코드 커밋**(사용자 확인) 후 푸시 → 원격 CI 로 AOT 4종 매트릭스 확증
2. 사용자 결정 3건(라이선스·지원 정책·서명) — 풀리면 발행 준비
3. Phase 22 잔여 — K8s 실클러스터 검증(클러스터 확보 시) · 전 Phase 게이트 재확인 ·
   문서 전체 검토(DocFX 경고 58건 포함)

---

# 2026-08-12 (이어서) — 커밋·푸시 + 🟢 CI 초록 + 드레인 플레이크 종결

## 한 일

- **오전 세션 산출물 커밋·푸시** (사용자 승인) — 3분할: 메타 패키지(`1a7f30d`) →
  샘플 AOT 4종 편입 + SIGTERM(`7b032d2`) → 컨테이너·K8s 예제(`a6c53a9`)
- **첫 CI 실행(31550966690)에서 ubuntu 만 실패** — windows 는 AOT 4종 완주 확인.
  실패는 `DrainOrchestrationTests.Drain_flipsReadinessImmediately_...` 1건:
  전파 창 600ms 안에서 `Task.Delay(200)` 후 접속하는 구조라, 러너 멈칫이 창을
  넘기면 언바인드 이후 접속 → [듣고 있는 서버가 없다]. **8/11 부터 감시하던
  [이름 미포착 1회성 로컬 실패]와 같은 부류로 판단, 종결**
- **드레인 전파 창 정비** (`bf2f913`) — 창 600ms → 4s, 접속 전 대기 200ms → 50ms.
  계약([창 안에서는 받는다])은 불변, 로컬 5회 반복 통과. 8/11 플레이크 정비와 같은 원칙
- ⭐ **CI 완전 초록** (실행 31551677209 @ `bf2f913`) — 3잡 동시 통과.
  **ubuntu 가 linux-x64 AOT 4종 publish + 실행 검증 완주 → 양 OS × 4종 매트릭스 완성.**
  메타 패키지 포함 33개 프로젝트 솔루션이 전체 게이트 통과
- 부수 확인: PS 5.1 에서 커밋 메시지 안 큰따옴표가 네이티브 인자 경계를 깨
  `-m` 이 조각난다 — 메시지에 따옴표를 넣지 않는 것으로 회피

## 결정

- 없음 (플레이크 정비는 기존 원칙의 적용 — 아키텍처 결정 아님)

## 측정

- 없음

## 막힌 것 / 미완료

- K8s 실클러스터 apply·rollout — 로컬 클러스터 부재(변동 없음)
- 사용자 결정 3건 대기: 라이선스(발행 선결) · 지원 정책 · 패키지 서명

## 다음

1. 사용자 결정 3건 — 라이선스가 정해지면 패키징 메타데이터 반영 + 발행 준비
2. Phase 22 잔여 — 전 Phase 게이트 재확인 · 문서 전체 검토(DocFX 경고 58건) ·
   최종 보안 검토 · 최종 성능 기준선 공표 · K8s 실클러스터 검증(클러스터 확보 시)
3. 릴리스 시점 작업(첫 릴리스 후): API 호환성 CI · Shipped 확정 · 템플릿 전환

---

# 2026-08-12 (이어서 2) — DocFX 경고 0 + 전 Phase 게이트 1차 재점검

## 한 일

- **DocFX 경고 58건 전량 해소 → 0건** (코드 미커밋 2파일) — 정체가 문서에 적힌
  [cref 수준]이 아니었다. 전수 분류: **전부 인프라 경고**
  - 68건 [Duplicate source file]: PublicApiAnalyzers 의 buildTransitive targets 가
    `PublicAPI.*.txt` 를 자동 포함(패키지 소스 실측 5.6.0)하는데
    `Server/Directory.Build.props` 가 수동으로 또 포함 → **수동 포함 제거**
  - 2건 [project ref without metadata ref]: 메타 패키지가 docfx 제외 대상
    (Analyzers·SourceGen)을 참조(어제 +2 의 원인) → **docfx 에서 메타 제외**
    (문서화할 API 가 없다)
  - 검증 3중: ① **RS0016 음성 테스트** — 제거 후 미승인 public 심볼 추가 시 여전히
    발화(게이트 생존 — [게이트가 조용히 사라진다]는 이 저장소 최경계 실패 형태)
    ② DocFX 전체 재빌드 경고 0 · API 페이지 336개 ③ 전체 솔루션
    `--no-incremental -warnaserror` 재빌드 경고 0
- **전 Phase 게이트 1차 재점검** (Phase 22) — 게이트 13개(Phase 0~12) 전수:

  | Phase | 게이트 요지 | 증거 (2026-08-12 기준) | 판정 |
  |---|---|---|---|
  | 0 | CI + API 게이트 | CI 3잡 초록(31551677209) + RS0016 음성 테스트(오늘) | ✅ |
  | 1 | Core 무의존·XML 문서 | CHSM0001+CoreDependencyTests · -warnaserror 빌드 | ✅ |
  | 2 | 핸들러 전송 중립 + AOT | CrossTransport 14×5 · AOT 4종 양 OS(오늘) | ✅ |
  | 3 | 풀 0할당·누수 감지 | 할당·누수 게이트 테스트 상시 · 8/11 1만 재실측 | ✅ |
  | 4 | 퍼징·프레임당 0할당 | 퍼즈 테스트 6파일 · 할당 게이트 상시 | ✅ |
  | 5 | 1만 접속·p99 기준선 | BENCHMARKS 기준선(2026-08-08 확정) | ✅ |
  | 6 | 직렬화 어댑터 2+ ·기본값 ADR | 어댑터 3종 + 샘플 2종 교차 · ADR-0013 | ✅ |
  | 7 | 생성 코드 AOT·라우팅 우위 | AOT 4종 CI + bench-gate 라우팅 비율(상시) | ✅ |
  | 8 | 코어 확장 선형·순서 보장 | 기준선 14.67×/16코어 + 순서 반복 테스트 | ✅* |
  | 9 | 위협 완화 매핑·인증 전 차단 | THREAT-MODEL 22위협 + 차단 게이트 테스트 | ✅ |
  | 10 | 과부하 거부 생존·24h soak | 복합 폭주 테스트 + soak 짧은 판 상시 | ⚠ 부분 |
  | 11 | 관측 오버헤드 기록 | ~72ns/0B 실측(BENCHMARKS) | ✅ |
  | 12 | 회귀 게이트 실효성 | 고의 회귀 검증 완료 + CI 상주 | ✅ |

  잔여 2건은 의도된 수동/장기 실행: **24h soak 정식 판**(P10) ·
  **확장성 5지점 곡선 재실행**(P8/12 — *컨테이너 4종 상주 중인 개발 세션에서는
  측정 노이즈로 거짓 실패를 만들 수 있어 의도적으로 실행 안 함. 릴리스 직전
  조용한 환경에서)

## 결정

- 없음 (중복 제거·설정 정리는 기존 결정의 정정 — ADR-0067 의 [58건 cref 수준]
  표기는 ROADMAP 에서 정정, ADR 본문은 추가 전용 규칙대로 불변)

## 측정

- 없음

## 막힌 것 / 미완료

- **⚠ 코드 미커밋 2파일** — `Server/Directory.Build.props` · `docs/docfx/docfx.json`.
  제안: `build(eng): DocFX 경고 58건 전량 해소` 단일 커밋 (사용자 확인 후)
- 24h soak 정식 판 · 확장성 곡선 재실행 — 위 참조
- 사용자 결정 3건 대기(변동 없음)

## 다음

1. 미커밋 2파일 커밋(사용자 확인) → 푸시 → CI 확인
2. 사용자 결정 3건(라이선스·지원 정책·서명)
3. Phase 22 잔여 — 문서 전체 검토(죽은 링크·낡은 예제) · 최종 보안 검토 ·
   확장성 곡선/24h soak(조용한 환경에서) · 최종 성능 기준선 공표

---

# 2026-08-12 (이어서 3) — ⭐ 라이선스 Apache-2.0 확정 (사용자 결정 1/3 해소)

## 한 일

- **DocFX 수정 커밋·푸시** (`c8d874f`) — 이전 세션 미커밋 2파일. CI 초록
  (실행 31557787748) — PublicAPI 수동 포함 제거가 33개 프로젝트 -WarnAsError
  빌드·API 게이트에 무영향임을 원격에서 확증
- **사용자 결정 3건 상세 설명** — 라이선스(수익 모델이 근본 질문)·지원 정책
  (기간 숫자 2개)·서명(인증서 구매 vs 무료 CI 출처 증명, attestation 은 공개
  저장소 전제 — 현 private 확인). CI 출처 증명 절차 상세 설명(Sigstore·OIDC
  단명 인증서·Rekor — 키 관리 없음)
- ⭐ **라이선스 확정: Apache-2.0** (**사용자 결정**, `653af67`, ADR-0071) —
  MIT 대비 특허 허여·보복(3조)·상표 보호(6조)·기여 조항(5조)이 채택 근거
  - `LICENSE`(정본 전문)·`NOTICE` 신설 — 저작권 표기 "The ChServerM Authors"
    (관례 채택, 명의 확정 시 교체 가능)
  - 패키징: `PackageLicenseExpression=Apache-2.0` + `Copyright` + **전 패키지
    LICENSE·NOTICE 동봉**(4조 (d)). 33개 pack 실측 — nuspec expression 실림·
    동봉 누락 0
  - **서드파티 전수 감사** `THIRD-PARTY-NOTICES.md` — 중앙 패키지 전 항목 +
    Server 어셈블리 **전이 의존성 포함** 그래프를 nuspec 실물에서 판독,
    URL 표기 구세대는 원문 확인(K4os=MIT). **판정: 충돌 0** — 카피레프트 0건,
    런타임 전부 MIT·BSD-3·Apache-2.0·PostgreSQL. MS-PL 1건은 테스트 전용
  - CI 초록(실행 31562749224) — **발행의 법적 선결 조건 해소**

## 결정

- **ADR-0071** — 라이선스 Apache-2.0 (사용자 결정). 대안 탈락: MIT(특허·상표·
  기여 조문 침묵)·듀얼(GPL 회피 수요 약함)·BSL(발행·저변과 상충). 파일별 헤더
  보일러플레이트는 미채택(한글 주석 규약과 충돌, 효력 무관)

## 측정

- 없음

## 막힌 것 / 미완료

- **사용자 결정 잔여 2건**: ② 지원 정책(지원 버전 범위 + 보안 패치 기간 —
  권고: 최신 minor + 직전 minor 보안 6개월) ③ 서명(무료 1단계 attestation 이
  유력하나 **저장소 공개 전환이 전제** — 공개 시점 결정 필요)
- 저작권 명의 — "The ChServerM Authors" 관례로 시작, 법인·개인 확정 시 교체
- 24h soak · 확장성 곡선 · K8s 실클러스터 — 변동 없음

## 다음

1. **지원 정책 결정**(숫자 2개) → SECURITY.md + VERSIONING.md 반영은 자율 작업
2. **저장소 공개 전환 시점 결정** → release.yml(pack+attestation) 골격은 자율 작업
3. Phase 22 잔여 — 문서 전체 검토 · 최종 보안 검토 · 조용한 환경에서
   확장성 곡선/24h soak

---

# 2026-08-12 (이어서 4) — ⭐⭐ v0.1.0 첫 발행: nuget.org 에 33개 전부

## 한 일

- **지원 정책 확정** (**사용자 결정** = 권고안, `50c6f9b`, ADR-0072) —
  SECURITY.md 신설(정본) + VERSIONING.md 연결 절. CI 초록
- **릴리스 파이프라인 골격** (`81e034e`) — release.yml: 태그 → 게이트(build.ps1
  전체) → pack(CIB) → SLSA 출처 증명(공개 저장소 조건 가드) → 산출물 보존.
  **README 신설**(`c229aa1`, 공개 전환의 전제 — 지금까지 없었다)
- **리허설 1차가 플레이크를 잡음** → **TTL 판정 불능 Skip 가드** (`877afd5`) —
  8/11 의 4배 스케일로도 Redis 판 재발. 러너 멈칫 상한(5s+, f3fd254)이 어떤 고정
  여유보다 클 수 있으므로, 판정이 깨졌고 실측 경과 ≥ TTL 90% 면 판정 불능으로
  Skip(타이밍 정상 시 엄격성 불변·가짜 시계 스토어는 가드 미발화). 리허설 2차 초록
- **발행 인증을 Trusted Publishing 으로** (`ed7125a`, ADR-0073) — **사용자 지적**
  ("API 키는 권장되지 않는데")이 맞았다. NuGet/login@v1(OIDC → 1시간 임시 키),
  장수명 시크릿 0, 발행은 태그에서만
- **저장소 공개 전환 + PVR 활성화** — 공개 전 **레거시 자격증명 노출을 사용자에게
  명시 확인**(ServerGlobals.cs:103 의 mongodb://smck:smck4@localhost — 8/11 의
  "계정 폐기 확인" 결정은 비공개 전제였으므로 재확인이 필요했다) → 승인 후 전환
- ⭐⭐ **v0.1.0 첫 발행** — 태그 푸시 → 1차 401(정책 필드 불일치, 오류 메시지가
  정확히 안내) → 사용자 정책 수정 → 실패 잡만 재실행 → **전 스텝 초록**.
  검증 3중: ① nuget.org 색인 33/33 전수 확인 ② **공개 피드만으로 실소비**
  (nuget.org 단독 소스 + 격리 캐시에서 메타 복원 → 조립·기동·생성 코드 왕복)
  ③ 출처 증명 소비자 검증(`gh attestation verify`) 통과
- **출처 증명 검증 대상 정정** (`6683691`) — nuget.org 는 업로드본에 저장소
  서명을 덧붙여 다운로드본 해시가 달라진다(실측 9FEC…↔1876…). 검증 대상은
  워크플로 아티팩트, 다운로드본은 dotnet nuget verify 몫

## 결정

- **ADR-0072** — 지원 정책(사용자 결정): 최신 minor 전부 + 직전 minor 보안 6개월
- **ADR-0073** — 발행 인증 Trusted Publishing: 장수명 API 키·저자 서명 인증서 탈락
- 저장소 공개 전환(사용자 결정, 자격증명 노출 확인 포함) · Trusted Publishing
  정책 등록·NUGET_USER(=whoomch) 시크릿(사용자)

## 측정

- 없음 (발행 세션)

## 배운 것

- **⭐ 리허설은 밥값을 한다** — dispatch 리허설 1차가 실발행 전에 플레이크를
  잡았고, 2차가 파이프라인 전체를 초록으로 실증한 뒤에야 태그를 밀었다
- **⭐ 사용자 지적이 설계를 고쳤다** — API 키 초안 → Trusted Publishing.
  "권장 방식"은 시점의 함수다: 문서를 다시 읽는 것이 맞았다
- **⭐ 공개 전환은 이전 결정의 전제를 뒤집는다** — "저장소 조치 없음"(8/11)은
  비공개 전제의 결정이었다. 전제가 바뀌면 결정을 다시 사용자에게 가져간다
- **⚠ 발행 파이프라인의 검증 대상은 업로드 전 바이트다** — 피드가 산출물을
  변형(저장소 서명)할 수 있음을 처음부터 가정해야 문서가 정확해진다

## 막힌 것 / 미완료

- 발행 후속(다음 자율 작업 후보): **템플릿 PackageReference 전환** ·
  **GETTING-STARTED/README 의 [미발행] 문구 갱신** · **API 호환성 CI 활성화**
  (기준선 0.1.0 생김) · GitHub Release 노트 생성(eng/release-notes.ps1)
- 게이트 잔여 2건(24h soak · 확장성 곡선)과 K8s 실클러스터 — 변동 없음

## 다음

1. **발행 후속 4건**(위) — 전부 자율 작업 가능
2. Phase 22 잔여 — 문서 전체 검토 · 최종 보안 검토 · 조용한 환경 측정 2건
3. 1.0 시점 작업: Shipped 확정 · 전 Phase 게이트 최종 재확인

---

# 2026-08-12 (이어서 5) — 발행 후속 완결 + 문서 전체 검토

## 한 일

- **템플릿 메타 패키지 전환** (`c2c7464`) — ProjectReference(ChServerMRoot 파라미터)
  → `ChServerM` 한 줄. **종단 검증: nuget.org 복원만으로** 설치→생성→빌드→서버
  기동+클라이언트 에코 왕복→제거 통과
- **문서 발행 반영** (`de54709`) — GETTING-STARTED 는 `dotnet add package` 경로로
  (클론은 샘플 실행에만), README 는 발행됨 링크로
- **API 호환성 게이트 활성화** (`b69581c`) — 기준선 0.1.0 + build.ps1 pack 단계.
  **양방향 검증**: 음성(가짜 기준선 0.0.1 → NU1102 실패 = 속성이 조용히 무시되지
  않음) + 양성(33개 전부 통과). Phase 21 항목 완료
- **GitHub Release v0.1.0** — 큐레이션 노트(설치·구성·실측·검증 명령) + **자산
  63개 = 발행 원본 바이트**(attestation 영구 검증 대상 — 워크플로 아티팩트 90일
  만료 문제 해소). 아티팩트 2개(1차 실패분·재실행분) 중 재실행분을 식별해 첨부
- **틱-휠 조립 테스트 측정 원점 수정** (`696a597`) — [너무 일찍 발화 32ms]는 휠
  결함이 아니라 **시계를 예약 뒤에 켠 측정 결함**(마감은 TrySchedule 내부 시각
  기준, 콜드 러너 첫 호출 JIT ~18ms 만큼 일찍 측정). 원점을 예약 전으로. 로컬
  5회 통과, CI 초록
- **문서 전체 검토 1차** (`3e5c3fe`, Phase 22 항목 완료) — 링크 전수 61개 md →
  깨진 링크 0(레거시 인덱스 7건 수정) · 낡은 서술 정정 · ARCHITECTURE 에
  Matchmaking 행 + 메타 패키지 추가
- **⚠ 사고와 복구** — PS 5.1 정규식 치환이 props 파일을 CP949 오독 재기록으로
  파괴(이 저장소가 문서화한 그 함정을 직접 밟음) → git 복원 + Edit 도구 재적용.
  **교훈: 한글 파일의 기계 치환은 반드시 UTF-8 명시 읽기 or Edit 도구로**
- CI 초록 3회(마지막 3e5c3fe) — pack 게이트 포함 매트릭스 안정

## 결정

- 없음 (릴리스 후속·테스트·문서 작업 — 아키텍처 결정 아님)

## 측정

- 없음

## 막힌 것 / 미완료

- Phase 21 잔여 1: `Shipped` 확정(1.0 시점). Phase 22 잔여: **최종 보안 검토**
  (새 세션 집중 권장) · 최종 성능 기준선 공표 · 조용한 환경 측정 2건(확장성
  곡선·24h soak) · K8s 실클러스터 · 전 Phase 게이트 최종 재확인(1차는 11/13)
- 템플릿 CI 자동 검증 미구성(수요 시)

## 다음

1. **최종 보안 검토** (Phase 22) — Phase 9 방식(다단계 리뷰) 재적용
2. 조용한 환경 확보 시: `eng/scaling-gate.ps1` 전체 곡선 + 24h soak
3. 1.0 준비가 무르익으면: Shipped 확정 → 전 Phase 게이트 최종 재확인 → 1.0

---

# 2026-08-12 (이어서 6) — ⭐ 최종 보안 검토 통과 + 확장성 게이트 재검증 + 게이트 도구 수정

## 한 일

- **최종 보안 검토 (Phase 22, 1.0 준비)** — 마지막 검토(`a1f9979`, Phase 9 마감) 이후
  HEAD 까지 283파일 / ~29k 삽입(Phase 10~22)을 Phase 9 방식으로 3단계 리뷰: 도메인별 병렬
  식별(세션·인증 토큰 / 비신뢰 바이트 파서 / 전송·TLS / 영속성·클러스터 / Core·Hosting) →
  후보별 위양성 필터 → confidence 8 미만 제거. **보고 임계치 초과 취약점 0건.**
  - 건전성 확인: 세션 재개(32B CSPRNG · SHA-256 저장 · `FixedTimeEquals` · CAS 회전으로
    도난·재전송 토큰 1회용화 · 재개 라우트가 pre-auth 기본거부 필터 통과) · 파서(공격자 길이
    필드 미사용, 고정 상수 대조) · LZ4(ADR-0019 해제 상한 선검증) · TLS(기본 전체 체인+호스트명
    검증, 1.3 전용, accept-all 콜백 부재) · Postgres(전 파라미터화 + 식별자 화이트리스트) ·
    Redis(단일키 Lua CAS, 숫자 전용 키) · 회로 차단기(죽은 저장소를 NotFound 로 접지 않음)
  - **보안 무관 신뢰성 버그 1건 발견(선택적 후속)** — `ConsulClusterMembership.BuildView` 가
    노드 ID 를 `ushort.TryParse` 로만 걸러 `MaxNodeId(1023)` 초과·중복 시 `NodeId`/`ClusterView`
    생성자 예외가 어느 catch 에도 안 걸려 멤버십 갱신 루프를 영구 정지시킨다(또는 기동 실패).
    위양성 필터 confidence 2/10 — 영향이 가용성뿐이고 트리거는 Consul 카탈로그 쓰기 권한
    (ACL 보호 신뢰 컨트롤 플레인)이라 보안 findings 제외. 수정 권고: 파싱을 `MaxNodeId` 로
    클램프 + 기존 skip-and-log 경로로 감싸기
- **확장성 게이트 5지점 재검증 (전 Phase 게이트 재확인 잔여 ② 완료)** — 환경 확인(Docker
  미실행 = 컨테이너 노이즈 해소 · 이 머신이 게이트 기준선 프로필 ENV-B(7945HX) 그 자체라
  어피니티 마스크 정합) 후 전체 곡선 실행. 1·2·4·8·16코어 전부 하한 이상, **16코어 14.90× /
  효율 93.1%** — 08-07 기준선(14.67×) 유지·소폭 상회. ADR-0005 전제 생존. BENCHMARKS.md 2026-08-12 절
- **게이트 도구 버그 수정 (`f7c508b`, 푸시됨)** — 첫 실행은 **거짓 통과**였다: exit 0 인데
  측정 0건. 어피니티가 프로세스 트리 전체(빌드 포함)를 1코어에 묶어, 08-07 이후 커진 솔루션의
  BDN 격리 클린 빌드(18개 프로젝트 + net9 FlatSharp)가 기본 2분 타임아웃을 넘겨 죽었고 결과
  JSON 은 NA 였다. `eng/scaling-gate.ps1` 에 `--buildTimeout 900` 추가(이 스크립트 전용 —
  CI bench-gate 는 코어 핀을 안 하므로 무관). 교훈: **게이트 exit 0 ≠ 통과, 산출물(배수 표)로 확인**
- **Shipped 확정(3번)은 착수하지 않고 표면 점검만** — VERSIONING.md 는 `Unshipped → Shipped`
  전량 이동을 **1.0 선언과 동시**로 못박는다(절차 3, v1.0 태그 직전). 현재 0.1.0 이고 1.0
  선결 게이트(24h soak · 최종 기준선 공표 · 전 Phase 재확인)가 미완이라, 지금 동결하면 남은
  pre-1.0 API 변경이 자기 유발 파괴가 된다 → 실행하지 않고 사용자 결정으로 남김. 읽기 전용
  점검 결과: **공개 표면 2,280줄 / 32개 어셈블리 전량 Unshipped, Shipped 0줄** — 동결은 1.0
  시점의 단일 move-all 작업
- 커밋·푸시: `f7c508b` (게이트 도구 수정) 푸시 완료(`d0a3b50..f7c508b`)

## 결정

- 없음 (ADR 없음 — 게이트 빌드 타임아웃 상향은 아키텍처 결정이 아니다. 최신 ADR-0073 유지)

## 측정

- 확장성 게이트 5지점(ENV-B): 16코어 **14.90× / 효율 93.1%**, 전 지점 하한 이상.
  상세 표·함정은 BENCHMARKS.md 2026-08-12 절

## 막힌 것 / 미완료

- **24h soak**(`CHSM_SOAK_SECONDS=86400`) — 장기 실행이라 별도 세션 배치 필요
- **최종 성능 기준선 공표** — soak + 전체 job 측정과 함께
- **Shipped 확정 · 1.0 태그** — 1.0 선언 결정 대기(사용자). 되돌릴 수 없는 동결이라 선결 게이트 후
- (선택) Consul `BuildView` 신뢰성 버그 수정

## 다음

1. **24h soak** 별도 세션 → 완료 시 최종 성능 기준선 공표
2. 1.0 준비 완료 판단 시(순서 고정): 표면 점검 → Shipped 전량 이동(2,280줄) →
   VersionPrefix 1.0 → 전 Phase 게이트 최종 재확인 → v1.0 태그
3. K8s 실클러스터 apply·rollout 검증(클러스터 확보 시)
4. (선택) Consul `BuildView` 신뢰성 버그 수정
