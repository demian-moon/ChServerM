# 2026-08-06

> 세션 시작 시 7차(2026-08-05) wrap 누락을 소급 처리했다(`b82a1fa`) — 그 기록은
> `2026-08-05.md` 7차 절에 있다. 이 파일은 오늘 세션의 신규 작업분이다.

## 한 일

### 1. 버전 협상 핸드셰이크 와이어 구현 — ADR-0017 잔여 해소 (`2fcb199`, `6d70037`)

- **`VersionHandshakeCodec`(Core 신설)** — `ClientHello`(40005, 헤더16B+`[Min,Max]`4B) →
  `ServerHello`(40006, +2B) / 거부 = `ConnectionRejected`(40004, +사유·서버구간 6B, R-3)의
  **영구 동결** 코덱. 프레이밍 어댑터가 아니라 Core 인 이유: 부트스트랩을 교체 가능한
  축에 얹으면 축 교체가 와이어를 깨는 모순(R-2) — varint 와이어에는 버전 필드 자체가
  없음을 확인. 고정 헤더 v1 레이아웃의 의도적 중복은 `FrameHeaderCodec` 교차 검증
  테스트가 지킨다. 파싱은 엄격(전 고정 필드 검증), 실패는 값(T-16)
- **호스팅 통합** — `UseVersionNegotiation()` 양쪽 빌더. 서버는
  `Secured(→ Negotiating(→ Framed))` 감싸기로 **협상이 TLS 채널 안**임을 조립이
  강제(R-4 구조 충족). 핸드셰이크가 정확히 자기 바이트만 소비 — 파이프라인으로
  뒤따라온 앱 프레임 유실 없음. 무응답 = 타임아웃 절단(T-16), 결과 =
  `IProtocolVersionFeature` + 버전별 로그(R-5, 카운터 승격은 Phase 11)
- 클라이언트 거부 = `VersionNegotiationException`(서버 지원 구간 포함 — "업데이트 필요"
  안내 근거). 종단 테스트 8종(합의/거부+생존/비협상 첫 프레임/무응답 타임아웃/TLS 안
  협상/동결 교차 검증), 5회 반복 경합 없음

### 2. 인증 축 — T-05·T-20 완화 완결 (`aff7942`, `9a7297d`, `f47967b`, `2cabad9`)

- **설계 제시 → 사용자 재검토 요청 → 적대적 재검토로 4점 보강 → 4개 결정 승인** 후 구현:
  ① `AuthenticateAsync(MessageContext)` 단일 인자(토큰 중복 제거) ② 페이로드 수명 계약
  명시(await 전 복사 — 레거시 ToArray 위반의 재발 방지) ③ 리플레이 가드
  **검증→클레임 순서** 계약(역순은 쓰레기 토큰으로 가드 포화 = 로그인 DoS)
  ④ 조립 순서 검증(인증이 필터보다 바깥이면 `Build()` 예외)
- **`AuthenticationMiddleware`** — 실패 = `next` 미호출 + **옵션 무관** 6000 종료.
  전용 `DispatchStatus.RejectedByAuthentication`(8) 신설: `RejectedByPolicy` 재사용 시
  `CloseOnPolicyRejection` 하나로 인증 실패가 통과하는 구멍(T-20 재발 경로).
  성공 = `GrantedStates` **상태 대체 전이** — "인증됨" 별도 플래그를 두지 않아
  인증 여부와 허용 메시지 집합이 어긋날 표면 제거(T-19 필터와 한 몸)
- **`InMemoryTokenReplayGuard`** — 유계(포화 = 신규 거부 + 경고, 9.6) + TTL(메모리
  유계 장치). 동시 클레임은 원자적 `TryAdd`/`TryUpdate` 로 정확히 하나만 승리,
  정리는 타이머 없이(9.5) 포화 시점에만 + `Interlocked` 게이트 해제는 `finally`(9.2).
  "같은 토큰 두 번째 커넥션 거부"가 T-05 실증 테스트
- **`ChServerM.Security.AspNetIdentity` 신설(ADR-0018)** — 레거시 유일 승계 자산
  (`AuthM` 의 `PasswordHasher`)의 어댑터. 같은 라이브러리·형식이라 **기존 계정 해시
  그대로 검증**(테스트 고정 — 계정 이전 경로). 결함 4종 역해소: 1회 생성 재사용 /
  반복 횟수 생성자 명시(기본 OWASP 600,000) / 공개 축 / `SuccessRehashNeeded` 표면화

## 결정

- **ADR-0018** — 비밀번호 해싱은 `PasswordHasher<T>`(V3) 위임.
  탈락: `Rfc2898DeriveBytes` 직접 형식(형식 재발명 + 레거시 호환 상실) /
  Argon2id(표준 구현 부재 + 전 계정 재설정 강제 — 버전 태그로 점진 전환 문은 열림)
- `DispatchStatus.RejectedByAuthentication` 신설 (T-20 을 옵션이 아닌 불변으로)
- 리플레이 가드 이번 범위 포함(계약+인메모리), PasswordHasher 승계 포함 — 전부
  설계안 4개 결정 지점으로 사용자 승인

## 측정

- 없음 (보안 기능 증분 — 인증은 커넥션당 1회라 핫패스 아님. TLS on/off 실측은 어제 완료)

## 배운 것 (같은 실수 반복 방지)

- **"최선인가?"라는 질문에는 방어가 아니라 공격으로 답한다** — 재검토에서 시그니처
  중복 인자, 페이로드 수명 위반 경로, 가드 포화 DoS, 조립 순서 함정 4개를 스스로
  찾았다. 첫 제안을 방어했으면 전부 구현 후에 발견됐을 것들이다
- **벤더 예외는 어댑터가 값으로 변환한다** — `PasswordHasher` 가 base64 아닌 손상
  해시에 `FormatException` 을 던진다는 것을 테스트가 잡았다. "오염된 저장소 = 값
  실패"(T-16) 계약은 어댑터 변환 계층의 몫이다
- **동결 계약의 의도적 중복은 교차 검증 테스트와 쌍으로** — Core 코덱과 Framing
  코덱의 레이아웃 일치를 사람이 아니라 테스트가 지키게 했다

## 막힌 것 / 미완료

- 없음 (작업 트리 clean). Phase 9 잔여(게이트 조건 아님): 인가 미들웨어(T-21,
  `IAuthorizationPolicy`) / `IPayloadCodec` 압축(T-11·18) / 시크릿 관리 / 입력 검증 /
  Tls 인증서 파일 로딩·회전 운영 경로 / `/security-review`
- 인증 실패의 클라이언트 와이어 통지 없음(종료만 관측) — Phase 10 거부 통지 체계와
  묶어 설계(의도적 보류)
- 테스트 수 집계 기준 변경: 문서상 "423개"는 실측(443)과 20개 차이가 있었다 —
  이번부터 `dotnet test` 전 스위트 합산 기준(현재 **524개**)으로 기록

## 다음

1. **인가 미들웨어(T-21)** — `IAuthorizationPolicy` 계약 + 메시지별 권한 검사.
   Phase 1 잔여 계약의 마지막 인증·인가 몫. 설계 제시 → 승인 → 구현 순서
2. `IPayloadCodec` 압축(LZ4/Zstd) — 압축→암호화 순서 고정(T-11), 해제 출력 상한
   계약 포함(T-18: 구현체가 생략할 수 없게)
3. (후보) Tls 인증서 파일/저장소 로딩·회전 운영 경로

---

# 2026-08-06 (2차)

## 한 일

### 인가 미들웨어(T-21) — Phase 1 인증·인가 계약 마감 (`792ea9c`, `83a8d76`)

- **설계 제시 → 3개 결정 승인**(구현 진행 / 보호 목록 방식 / 거부 = 옵션 게이트) 후 구현.
  범위 판정을 정직하게: 메시지 수준 거친 인가는 T-19 필터 + 인증 `GrantedStates` 가
  **이미 기본 거부로 담당** — 이 증분의 실체는 상태 비트로 표현 불가한
  **자원 수준 판정**(페이로드 의존 소유자 검사, 동적 정책)이다. "문서만 갱신하고
  보류" 선택지도 제시했고, 구현이 선택됐다
- `IAuthorizationPolicy`(Core) + `AuthorizationDecision`(default = 거부, 사유 로그 전용) —
  신원은 인증기가 남긴 커넥션 피처에서 읽는다(조립 순서 근거)
- `AuthorizationMiddleware`(Hosting) — `Protect()` 보호 목록(FrozenSet 조회 1회,
  비보호 메시지는 정책 비용 0). 거부 = 기존 `RejectedByPolicy`(6001) +
  `CloseOnPolicyRejection` 옵션. **인증(옵션 무관 종료)과 의도적 비대칭** —
  인가 거부는 정당한 세션의 정상 흐름일 수 있다(권한 밖 클릭). 거부는 항상 경고 로그
- 조립 순서 검증 3자 확장: **필터 → 인증 → 인가**, 위반별 근거를 예외 메시지에.
  빈 보호 목록 = 죽은 조립 예외
- 종단 테스트 +15(단위 4 + 종단 11, 2전송): 소유자 불일치 = 기본에서 무응답 +
  커넥션 생존(후속 정당 요청 정상 처리로 증명) / 엄격 옵션 = 6001 종료 /
  비보호 메시지 정책 호출 0회 / 조립 검증 5종
- THREAT-MODEL T-21 ✅ / ROADMAP 인가·Phase 1 계약 체크. **1차 wrap 편집 실수로
  ROADMAP 인가 항목 줄이 삭제됐던 것을 발견·복원**(진행률 정정 88/222)

## 결정

- 없음 (ADR 신규 없음 — 설계 결정 3건은 대화 승인, 근거는 커밋 본문·모듈 주석)

## 측정

- 없음 (비보호 메시지 경로는 FrozenSet 조회 1회 — 전체 게이트 통과로 확인)

## 배운 것

- **여러 줄 old_string 치환은 삭제를 만들 수 있다** — IAuthenticator 체크 편집에서
  연속된 다음 줄(인가 항목)을 new_string 에 빠뜨려 조용히 지웠다. 여러 줄 치환 후에는
  주변 줄 보존을 확인한다

## 막힌 것 / 미완료

- 없음 (작업 트리 clean, 전 스위트 539개 통과)

## 다음

1. **`IPayloadCodec` 압축**(LZ4/Zstd) — 압축→암호화 순서 고정(T-11), 해제 출력
   상한을 계약 인자로(T-18). Phase 1 잔여 계약 + Phase 9 항목 동시 해소
2. Tls 인증서 파일/저장소 로딩·회전 운영 경로
3. (후보) 시크릿 관리 / 입력 검증·퍼징 확대

---

# 2026-08-06 (3차)

## 한 일

### 압축 축(IPayloadCodec) 완결 — T-11·T-18 완화 (`c04644b`, `3507b0e`, `b9bd2d0`, `36f0e9d`, `2030489`)

- **설계 제시 → 3개 결정 승인**(구현 진행 / 첫 어댑터 K4os LZ4 / 벤치 포함) 후 구현
- **Core 계약** — `TryDecode(maxDecodedLength)` **필수 인자**: T-18 상한 검사를 구현체가
  생략할 수 없게 계약 표면에 고정. 블롭은 자기서술(원본 길이 접두) — **버퍼를 잡기
  전에** 선언 길이를 검증(레거시 T-12 의 역). 압축→암호화 순서(T-11)는 구조 보장:
  압축 = 페이로드 수준, TLS = 그 바깥 스트림 수준이라 뒤집힐 방법이 없다
- **`ChServerM.Compression.LZ4`(ADR-0019)** — K4os 1.3.8, FAST 고정.
  "압축이 실제로 실행됨"을 테스트로 고정(레거시는 `maxLength >= originDataLen` 항상
  참이라 무동작). LZ4 블록에 무결성 검사가 없음을 정직하게 문서화 — 무결성은 TLS
  AEAD 몫(T-04), 가짜 무결성 장치는 레거시 가짜 체크섬의 재판. 벤더 예외는 어댑터가
  값으로 변환(해셔 교훈 재적용)
- **Hosting 통합** — 송신 `WriteCompressedFrameAsync` 는 플래그를 스스로 붙인다
  (표시-변환 불일치 구조적 불가). 평문 판정 3갈래: 문턱(1024B 승계)/제외 목록
  (T-11 비밀 문맥)/이득 없음. 수신은 재조립 → 해제(FragmentAssembler 의 플래그
  보존이 예비한 조합), 상한 `MaxDecompressedMessageLength`(기본 1MiB). 미조립 +
  압축 프레임 = 종료(조용한 압축 바이트 전달이 레거시형 실패)
- **부수 경화** — 조각·압축 계약 위반 확정 시 같은 읽기 배치의 잔여 프레임 처리를
  즉시 중단(기존엔 조각 위반 후에도 배치 잔여가 디스패치된 뒤 닫혔다)
- 테스트 +21(단위 15 + 종단 6): 1GiB 선언 폭탄 = 할당 0 거부 + 서버 생존 /
  세그먼트 분할 전수 왕복 / 정책 3갈래(코덱 호출 여부까지 계수 검증) / 조각+압축

## 결정

- **ADR-0019** — 첫 어댑터 LZ4(K4os). 탈락: Brotli 첫 어댑터(비압축성 최악 경로
  11~35× 느림 — 실측) / Zstd(2번째 후보) / LZ4 프레임 형식(체크섬이 AEAD 와 중복) /
  varint 접두(어댑터 간 의존)

## 측정 (ENV-B, BENCHMARKS.md 압축 절)

- LZ4 인코드 582ns(1KB)~1.0µs(16KB, 압축성), 할당 0. Brotli(품질 1 최속 조건)는
  압축성 2.5~2.7×, **비압축성 11~35× 느림** — "이득 없으면 평문" 판정이 인코딩 후에야
  가능하므로 비압축성 최악 경로가 기본 코덱 선택의 결정타. 1024B 문턱 근거 확보

## 배운 것

- **PS 5.1 로 UTF-8 파일을 고치지 않는다** — `Set-Content` 치환이 한글 주석을 전부
  깨뜨렸다(ANSI 재해석). 파일 내용 수정은 Edit/Write 도구로만
- **압축이 잘 되는 테스트 데이터는 조각화 테스트를 무산시킨다** — 반복 패턴 16KB 가
  1KB 미만으로 줄어 조각이 안 생겼다. 조각+압축 조합 테스트는 비압축성(랜덤)으로
- **LZ4 블록엔 무결성이 없다** — 리터럴 손상은 같은 길이의 다른 내용으로 "성공"한다.
  코덱 테스트가 잡을 수 있는 것은 구조 손상뿐이고, 내용 무결성 주장은 AEAD 계층에서만

## 막힌 것 / 미완료

- 없음 (작업 트리 clean, 전 스위트 560개, 게이트 통과)
- 게이트 스크립트가 첫 실행에서 간헐 실패 후 재실행에 통과하는 현상 2회 관측
  (빌드 산출물 잠금 추정) — 재현 조건을 못 잡았다. 다음 발생 시 원인 추적

## 다음

1. **Tls 인증서 파일/저장소 로딩·회전 운영 경로** — `ChServerM.Security.Tls` 잔여
2. 시크릿 관리 — 설정 파일에 키를 두지 않는다(환경변수/시크릿 저장소)
3. 입력 검증·퍼징 확대 → 마치면 `/security-review` 로 Phase 9 마감

---

# 2026-08-06 (4차)

## 한 일

### Tls 인증서 로딩·회전 운영 경로 — Phase 9 Tls 항목 완결 (`886b2ab`)

- **설계 제시 → 3개 결정 승인**(구현 진행 / 핸드셰이크 시점 폴링 / 실패 시 기존 유지) 후 구현
- `IServerCertificateSource` — 핸드셰이크마다 인증서를 해석해 **회전이 재시작 없이
  새 핸드셰이크부터 반영**. 고정 인스턴스와 상호 배타. Core 불변(운영 관심사는
  어댑터 소속 — ADR-0017 표면 최소화)
- `FileCertificateSource` — PFX 단독/PEM 쌍(cert-manager 표준), **Windows Schannel
  ephemeral 키 함정을 PFX 왕복으로 내장 흡수**(실핸드셰이크 테스트로 회귀 방지).
  회전 감지 = mtime 폴링(기본 5분, 핸드셰이크 있을 때만 — 유휴 비용 0, 타이머·감시
  스레드 없음 9.5) + 명시 `Reload()`. FileSystemWatcher 탈락: k8s Secret 심볼릭 링크
  교체에서 이벤트 누락 악명
- 재적재 실패 = **기존 유지 + 경고 + 다음 주기 복구**(가용성 — 회전 실패가 장애가
  되면 안 된다), 시작 실패 = 예외. 구세대 1세대 보관(진행 중 핸드셰이크
  use-after-dispose 방지), 게이트 해제는 `finally`(9.2). 만료 임박(7일) 적재는
  경고 승격(회전 파이프라인 사망 신호, T-07)
- `PfxPassword` 는 환경변수/시크릿 저장소 문서 계약 — 시크릿 관리 항목과 연결
- 테스트 +8(전 스위트 568개), 게이트 통과

## 결정

- 없음 (ADR 신규 없음 — 새 의존 없음, 설계 결정 3건은 대화 승인·근거는 모듈 주석)

## 측정

- 없음 (핸드셰이크당 가상 호출 1회 + 주기 도래 시에만 파일 시각 조회 — 핫패스 아님)

## 배운 것

- **로드를 거친 인증서의 개인키는 재수출이 불가할 수 있다**(Windows 키 저장소 정책) —
  테스트 파일 소재는 로드 전 ephemeral 원본에서 직접 뽑아야 한다. `Export(Pfx)`
  재수출에 기댄 헬퍼가 전부 깨졌다
- 게이트 첫 실행 간헐 실패 3회째 — 캡처를 붙이면 통과해 재현 실패(직전 테스트
  호스트의 DLL 잠금 추정). 추적 계속

## 막힌 것 / 미완료

- 없음 (작업 트리 clean)

## 다음

1. **시크릿 관리** — 설정 파일에 키를 두지 않는다. 환경변수/시크릿 저장소 경로 +
   레거시 하드코딩 자격증명(`ServerGlobals.cs:103`) 블로커 항목과 연결
2. 입력 검증·퍼징 확대
3. `/security-review` 실행 + 결과 반영 → Phase 9 마감

---

# 2026-08-06 (5차)

## 한 일

### 시크릿 관리 — Phase 9 항목 완결 + 블로커 판정 (`3225133`, `6e22fcd`)

- **설계 제시 → 3개 결정 승인**(구현 진행 / 계약은 Core / 빈 값 = 부재) 후 구현
- 범위 판정을 정직하게: 프레임워크의 몫 = **소비의 단일 통로**(지점마다 환경변수
  읽기를 발명하다 리터럴로 퇴행하는 것을 막는다). 몫이 아닌 것 = 시크릿 저장소
  자체(KeyVault 어댑터 자리만) + **가짜 메모리 보안**(.NET 문자열은 지울 수 없다 —
  SecureString류는 가짜 체크섬의 재판이라 만들지 않음. T-10 현실 완화는 문서 계약)
- `ISecretSource`(Core — KeyVault류 미래 어댑터의 의존 방향) +
  `EnvironmentSecretSource`(프리픽스 이름 공간) + `DirectorySecretSource`
  (k8s 파일-퍼-시크릿, **캐시 없음** = 회전 즉시 반영, 끝 개행 제거 — "맞는데
  틀리는" 암호 사고, 경로 탈출 방어). **빈 값 = 부재** — "변수는 만들고 값을
  빠뜨린" 착각이 빈 암호로 조용히 진행되지 않게
- `FileCertificateOptions.PfxPassword` 를 참조 패턴 문서로 연계
- **블로커 판정 완료** — `ServerGlobals.cs:103` = `mongodb://smck:smck4@localhost:27017`
  (계정·암호 커밋). 저장소에 커밋된 순간 유출 간주, 로컬 개발 외 재사용됐다면
  폐기·교체 권고. 레거시 트리는 참조 전용 원칙대로 코드 불변
- 테스트 +8(전 스위트 576개), 게이트 통과

## 결정

- 없음 (ADR 신규 없음 — 새 의존 0, 설계 결정 3건은 대화 승인)

## 측정

- 없음 (시크릿 조회는 시작·재연결 시점의 저빈도 경로)

## 배운 것

- 게이트 첫 실행 간헐 실패 4회째 — 직전 `dotnet test` 직후에만 발생하고 캡처(시간차)를
  붙이면 통과. 테스트 호스트 잔존 프로세스의 파일 잠금 경합 추정 강화.
  eng/build.ps1 에 사전 정리 단계를 넣을지는 근본 원인 확정 후 판단

## 막힌 것 / 미완료

- 없음 (작업 트리 clean)

## 다음

1. **입력 검증·퍼징 확대** — Phase 9 마지막 구현 항목. 페이로드 필드 범위 검사
   패턴 + 기존 프레이밍 퍼징의 확장 범위 판단부터
2. `/security-review` 실행(사용자) + 결과 반영 → **Phase 9 마감**
3. 다음 Phase 후보: Phase 10 복원력(IRateLimiter·IAdmissionControl) 또는
   Phase 7·8 잔여 정리

---

# 2026-08-06 (6차)

## 한 일

### 입력 검증 훅 + 보안 축 퍼징 — Phase 9 마지막 구현 (`e437901`, `f1cd165`)

- **설계 제시 → 2개 결정 승인**(구현 진행 / 실패는 `DeserializationFailed` 재사용) 후 구현
- `IMessageValidator<T>`(Core): **역직렬화 성공 ≠ 유효한 값**(T-22). 스키마는 맞지만
  의미가 틀린 값(HP 음수·좌표 NaN·음수 수량)을 잡는다. 유효 범위는 워크로드 소관
  (ADR-0004)이라 프레임워크가 정할 수 없지만, **검증이 끼는 자리(역직렬화 직후·핸들러
  전)와 실패 규약(핸들러 미실행)**은 강제 — 핸들러 안 검증은 하나쯤 빠뜨리는 것이 기본값
- `Map` 4인자 오버로드 신설(serializer+validator+handler). 옵션 인자가 아니라 별도
  오버로드 — 기본값 있는 검증기는 "아무도 안 넣는 검증"(RS0026 원칙). 실패 =
  `DeserializationFailed` 재사용(범위 밖 = 스키마 어긋남과 같은 부류, enum 증식 없음)
- **퍼징 확대 2종**: 핸드셰이크 코덱 + LZ4 해제 — 무작위 5000회 + 유효 프레임 전
  오프셋×전 비트 변조에 **던지지 않음·상태 정의됨·출력 상한 준수·실패 시 미커밋**.
  시드 고정 재현
- 종단: 유효 메시지 핸들러 도달(2전송) / 범위 밖 값(스키마 완벽, 값만 범위 밖) =
  핸들러 미도달 + 커넥션 종료(2전송)

### ⚠ 게이트 간헐 실패의 실체 규명 + 수정 (`e437901`)

- **4·5차에서 "게이트 첫 실행만 실패, 재실행 통과 → 파일 잠금 추정"으로 오진했던
  현상의 진짜 원인**: 3차에 커밋한 `CompressionBenchmarks` 가 CA1707(밑줄 6건)·
  CA5394(`System.Random` 1건)를 위반하는데, 벤치 프로젝트가 **증분 빌드에서 스킵**돼
  분석기가 안 돌다가 클린 빌드에서만 `-WarnAsError` 로 터졌다. "간헐"이 아니라 **결정적
  실패**였고, 캡처를 붙일 때 통과한 건 그 사이 다른 빌드가 산출물을 갱신했기 때문
- 수정: 메서드명을 밑줄 제거 PascalCase + `[Benchmark(Description=...)]`(기존
  `DispatchBenchmarks` 규약 일치), 비압축성 채움을 splitmix 곱셈 해시로(재현성 +
  CA5394 회피). 3차 벤치 커밋의 결과 수치는 유효(런타임 로직 불변)

## 결정

- 없음 (ADR 신규 없음 — 설계 결정 2건은 대화 승인)

## 측정

- 없음 (검증은 필드 비교 수준 — 핫패스 할당 0. 별도 벤치 없이 게이트로 확인)

## 배운 것

- **"간헐적"이라는 진단은 대개 관측 조건의 차이다** — 4·5차에 "파일 잠금 추정"으로
  두 번 미룬 것이 실은 증분 빌드가 분석기를 건너뛴 결정적 실패였다. 재현 안 되면
  "간헐"로 분류하기 전에 **빌드 산출물 상태(증분 vs 클린)**를 먼저 의심한다.
  이것이 2차의 "증분 빌드는 분석기를 건너뛴다" 교훈의 재발 — 그때는 CA2000 이었다
- **분석기 게이트를 믿으려면 CI·게이트는 클린 빌드여야 한다** — 반복 확인된 교훈.
  eng/build.ps1 이 이미 그렇게 도는데, 로컬의 개별 `dotnet build` 증분이 오진을 만들었다

## 막힌 것 / 미완료

- 없음 (작업 트리 clean, 전 스위트 596개). **Phase 9 구현 항목 전부 완료** —
  남은 것은 `/security-review`(사용자 명령)뿐

## 다음

1. **`/security-review` 실행**(사용자) — Phase 9 마지막 관문. 결과가 나오면 반영
2. Phase 9 마감 후: Phase 10 복원력(`IRateLimiter`·`IAdmissionControl` — 과부하
   거부·24h soak) 또는 Phase 7·8 잔여(누락 핸들러 검출·코어 제한 재측정) 중 택
3. `/security-review` 전이라도 착수 가능한 후보는 사용자와 함께 정한다

---

# 2026-08-06 (7차)

## 한 일

### `/security-review` 실행 — Phase 9 마감 (13/13)

- 사용자가 `/security-review` 실행. 브랜치 전체(origin 대비 45커밋, Phase 9 보안 축
  전량 + Part II 데이터 경로)를 3단계로 리뷰: 식별 서브태스크 → 후보별 병렬 위양성
  필터 → confidence 8 미만 제거
- **보고 임계치(>80% 악용 가능)를 넘는 신규 취약점 0건.** 검증되어 건전하다고
  확인한 경로 10종:
  1. `InMemoryTokenReplayGuard.TryClaim` — 원자적 `TryAdd` 단일 승자, 되살림은
     TTL 경과(계약상 이미 만료 토큰)에서만, 포화 시 fail-closed
  2. `AuthenticationMiddleware` — 실패 = `next` 미호출 + 옵션 무관 종료, 우회 불가
  3. `MessageStateFilterMiddleware` — 기본 거부 정확(미등록 → allowed=0 → 거부)
  4. `AuthorizationMiddleware` — 보호 목록 검사, 신원 피처 읽기가 인증 뒤임을 강제
  5. `EnsureKnownMiddlewareOrder` — 필터→인증→인가 위반 시 조립 예외
  6. `VersionNegotiatingConnectionHandler` — 핸드셰이크 바이트 정확 소비(유실·이중
     처리 없음), 엄격 프레임 검증
  7. `Lz4PayloadCodec.TryDecode` — 버퍼 확보 전 상한 대조, uint 비교 안전,
     불일치 시 Advance 전 중단
  8. `FileCertificateSource` — 2세대 보관(use-after-dispose 방지), 게이트 finally 해제
  9. `TlsTransportSecurity` — 기본 null 콜백 = 전체 체인+호스트명 검증(accept-all
     아님), TargetHost 필수, accept-all 콜백 부재
  10. `AspNetIdentityPasswordHasher` — V3 PBKDF2 600k 위임, 손상 해시 값 변환
- **임계치 미만 관찰 2건, 병렬 필터에서 각 confidence 2/10 위양성 확정**:
  - `DirectorySecretSource` 드라이브 상대 경로(`C:foo`) — 이름은 전부 조립 시점
    문자열 리터럴(비신뢰 입력 미도달), 배포 대상 k8s/Linux 에서 드라이브 의미 없음.
    하드닝 여지일 뿐(가드가 `:` 도 거부하면 됨), 악용 경로 없음
  - TLS 없는 버전 협상 다운그레이드 — 프레이밍 버전은 순수 와이어 레이아웃 선택자로
    보안 차등 없음(암호는 직교 축 TLS). 평문에서 잃을 보안 속성이 없고 현재 v1
    단일이라 다운그레이드 대상 자체가 없다. ADR-0017 결정 3 의 의도된 설계
- 리뷰 결과 코드 변경 불필요 — **Phase 9 보안 13/13 완료**

## 결정

- 없음 (리뷰가 기존 설계를 확인 — 코드·문서 변경 없음, 이 마감 커밋 제외)

## 측정

- 없음

## 막힌 것 / 미완료

- 없음. **Phase 9 완전 종료.** Part III 의 Phase 10(복원력)·11(관측)이 남았고,
  Part II 잔여(Phase 7 누락 핸들러 검출·리플렉션 폴백, Phase 8 코어 제한 재측정)도 있다

## 다음

1. **다음 Phase 착수 결정** — 사용자와 함께: Phase 10 복원력(`IRateLimiter`·
   `IAdmissionControl` — 과부하 거부·24h soak, "거부가 붕괴보다 낫다" 게이트) vs
   Phase 11 관측(`IMetricsSink` — 리플레이·인증 실패·버전 분포 카운터가 이미 로그로
   있으니 메트릭 파이프라인만) vs Part II 잔여 정리
2. 착수 전 해당 Phase 의 `docs/legacy/` 문서 선독(승계 대상이면)

---

# 2026-08-06 (8차)

## 한 일 — Phase 11 관측 착수, 메트릭 축 첫 증분 (게이트 충족)

### 순서 결정

- Phase 9 마감 후 다음 Phase 를 **Phase 11 관측 → Phase 10 복원력** 순으로 추천·승인.
  근거: (1) Phase 10 게이트("과부하 거부하며 생존 + 24h 메모리 평탄")가 측정을 전제 —
  관측 먼저 깔지 않으면 측정할 수 없는 복원력을 만든다 (2) Phase 9 가 조용한 실패 지점에
  EventId 를 이미 깔아 관측 한계 비용이 낮은 시점 (3) "실패가 관측되는가"가 Phase 11 의
  명시 목표 (4) Phase 10 리스크를 낮춘다

### 구현 (`ca3b331`, `79babde`, `4186d84`, `bf99ebf`, ADR-0020)

- **발견**: 메트릭 이름 계약(`DiagnosticNames`/`MetricNames`/`TagNames`/`ActivityNames`)이
  이미 Core 에 있었다(2026-08-04 정비). 없던 것은 축 계약과 어댑터
- **Core**: `IMetricsSink`(Count/Record/AdjustGauge) + `MetricTag`(문자열 값 전용 —
  박싱·카디널리티 사고를 타입이 막는다, 스팬 전달 무할당) + `NullMetricsSink`(기본값)
- **어댑터** `ChServerM.Observability`(BCL 전용, 새 의존 0): `MeterMetricsSink` 가
  `System.Diagnostics.Metrics.Meter` 위임. 첫 어댑터를 Meter 로 택한 근거 — dotnet-counters
  즉시, OTel 은 Meter 구독으로 얹으므로 재계측 아니라 배선만. 계측기 이름당 1회 캐시
- **Hosting 데코레이터**(CLAUDE.md 4 — 횡단 관심사는 코어 오염 금지):
  `MetricsConnectionHandler`(가장 바깥, 활성 게이지 감소는 finally 9.2) +
  `MetricsMiddleware`(파이프라인 가장 바깥, 지연·처리량·실패). `UseMetrics()` 한 번이
  둘을 올바른 순서로 배선
- 결정 3건 승인: 첫 어댑터 Meter / 데코레이터 우선 좁은 범위 / 어셈블리명 Observability

### 게이트 벤치 (Phase 11 게이트)

- 4변형 프레임당 비용(ENV-B): 기준선(관측 미조립) 6ns / Null 싱크 43ns / Meter 리스너
  없음 78ns / Meter+리스너 78ns. **전 변형 할당 0**
- 게이트 충족: 켠 오버헤드 ~72ns/프레임·할당 0, 끈 상태 6ns(기준선=끈 관측 비용 0 방어).
  비용 대부분은 Meter 가 아니라 미들웨어 async 래퍼(6→43ns) — 후속 최적화 후보로 기록

## 결정

- **ADR-0020** — 관측 첫 어댑터 `System.Diagnostics.Metrics`(BCL Meter) + 데코레이터 계측.
  탈락: OTel SDK 직접(새 의존 여럿·게이트 복잡) / `IMetricsSink` 없이 Meter 직접(축
  교체성·무의존 상실) / object 태그(박싱) / 프레임당 바이트 카운터(읽기 루프 오염, 후속)

## 측정 (ENV-B, BENCHMARKS.md 관측 절)

- 관측 오버헤드 4변형 — 위 게이트 벤치 참조. 켠 ~72ns·할당 0, 끈 6ns

## 배운 것

- **게이트가 관측을 전제하면 관측을 먼저 깐다** — Phase 10 을 먼저 했으면 측정 못 하는
  복원력을 만들 뻔했다. 게이트 조건을 읽고 의존 순서를 뒤집는 판단
- **데코레이터 async 래퍼가 계측 자체보다 비싸다** — Null 싱크에서도 6→43ns. 인터페이스
  싱크 가상 호출을 JIT 이 특수화 못 하고 async 상태 머신이 얹힌다. 트레이싱 증분에서도
  재등장할 비용이라 그때 fast-path 를 함께 판단
- **이름 계약을 미리 깔아둔 것이 지금 값을 했다** — 2026-08-04 에 메트릭 이름을 상수로
  정비해둬서 이번 증분은 축 계약과 배선만. 오타 하나가 대시보드 계약을 깨는 것을 막음

## 막힌 것 / 미완료

- 없음 (작업 트리 clean, 전 스위트 611개). Phase 11 은 다증분 — 이번은 메트릭 축 첫
  증분이고 게이트는 충족. 트레이싱·헬스체크·ZLogger·바이트/큐/풀 카운터는 후속

## 다음

1. **Phase 11 후속 증분 또는 Phase 10 착수** — 사용자와 함께 결정. 게이트가 충족됐으므로
   Phase 11 을 여기서 접고 Phase 10(복원력)으로 넘어가도 되고, 트레이싱·헬스체크를 더
   쌓아도 된다. 추천: **Phase 10 복원력** — 관측 토대가 생겼으니 이제 과부하 거부·soak 를
   측정 가능하게 만들 수 있다
2. 후속 관측 증분을 한다면: 프레임당 바이트·파티션 큐 깊이 카운터(읽기 루프/파티션 계측),
   분산 트레이싱(`ActivitySource`), 헬스체크 엔드포인트
