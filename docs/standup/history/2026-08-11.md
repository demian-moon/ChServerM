# 2026-08-11 — Phase 15 완료: Consul 어댑터와 노드 번호 임차

## 한 일

- **`PooledBufferWriter` 1만 커넥션 재측정** (`10c8d0b`) — ADR-0016 의 "정상 상태 할당 0"
  이 얕은 조건의 값인지 확인했다. **성립했다.** 벤치
  `Bench/ChServerM.Bench/Buffers/PooledBufferWriterScaleBenchmarks.cs`
- **Consul 멤버십 어댑터** (`03e728b`) — `ChServerM.Cluster.Consul` 신규 어셈블리.
  `HttpClient` + 소스 생성 JSON, **서드파티 의존 0**. 실제 Consul 컨테이너로 테스트 9종.
  `ARCHITECTURE.md` 에 클러스터 어셈블리 배치표 추가
- **노드 번호 임차** (`b396243`) — `ConsulNodeIdLease`. KV 잠금 + 세션(`Behavior=delete`).
  테스트 9종. **Phase 15 완료(17/17)**

## 결정

- **ADR-0055** — Consul 멤버십 어댑터. 블로킹 쿼리가 이 축의 계약과 **1:1** 이었다.
  벤더 패키지 대신 `HttpClient` + 소스 생성 JSON(리플렉션은 AOT 하드 룰을 깬다).
  ⚠⚠ **Consul 인덱스를 세대로 쓰지 않는다** — `ulong`↔`int` 절단 · **인덱스는 되돌아간다** ·
  무관한 이유로도 오른다. 세대는 **구성원이 실제로 달라졌을 때만** 오른다.
  etcd(헬스체크 없음 → 생존을 직접 모델링해야 함) · K8s(가짜 API 서버는 우리 가짜만 검증) 는 순서상 뒤로
- **ADR-0056** — 노드 번호 유일성: **배정하지 않는다. 겹침을 기동 실패로 드러낸다.**
  ⚠⚠ 상호 배제가 아니므로 프로세스를 대신 죽이지 않고 `Lost` 를 노출한다

## 측정

**`PooledBufferWriter` 1만 커넥션** (ENV-B, `OutstandingWriters` 가 유일한 변수)

| 경로 | 붙든 writer 1개 | 붙든 writer 10,000개 |
|---|---|---|
| **정상 상태**(`Clear` + 쓰기) | 53.69 ns · **0 B** | 68.59 ns · **0 B** |
| 생성·폐기 왕래 | 89.96 ns · 32 B | 78.84 ns · 32 B |
| 성장(작게 시작) | 160.08 ns · 32 B | 151.30 ns · 32 B |

## 배운 것

- **⭐⭐ 확인하러 갔다가 이전 결론의 <i>기준</i>이 틀렸음을 알았다.** ADR-0051 은
  "미처리 대여물이 **설정 용량**에 비례하면 함정" 이라 적고 `PooledBufferWriter` 를 같은
  성질로 분류했는데, 재 보니 아니었다.
  **붙들고만 있는 대여물은 버킷을 놓고 경쟁하지 않는다. 경쟁하는 것은 동시에 반납되는 것이다.**
  정정된 기준: *동시에 붙들린 수*가 아니라 **반납이 설정 용량 규모로 몰리는가**
- **⭐ 헤드라인이 진짜 비용을 가린다.** "할당 0" 은 맞지만 그 사용법의 실제 비용은
  **상주 메모리 80 MiB**(1만 × 8 KiB)다. 0 이라는 숫자가 그 크기를 안 보이게 한다
- **⭐⭐ 같은 실수가 다른 API 에서 또 나온다.** Consul 은 **잠금 획득 실패도 HTTP 200 +
  본문 `false`** 로 돌려준다 — ADR-0051 의 `PipeWriter.FlushAsync`(닫힌 상대에도 예외를
  던지지 않는다)와 **정확히 같은 모양**이다. 그때 적은 교훈("이 API 는 실패를 반환값으로
  알리는가를 API 마다 물어야 한다")을 이번에는 **미리 적용해서 처음부터 막았고**,
  고의 회귀가 그것을 확인해 줬다
- **⭐ 제공자의 "세대" 와 우리 "세대" 는 이름만 같다.** 그대로 실었으면 절단·되돌아감·
  헛깨어남 셋이 한꺼번에 들어왔을 것이다. **닮은 이름을 만나면 보장을 비교한다**
- **⚠ 어댑터에 등록 기능을 넣지 않은 것이 테스트를 살렸다.** 넣었다면 테스트가
  **어댑터가 쓴 것을 어댑터가 읽는 순환**이 됐다
- **⚠ 분석기가 측정 오류를 잡았다.** CA1859 가 벤치 `Fill` 의 인터페이스 매개변수를
  지적했는데, 그 루프는 측정 대상 안이라 인터페이스 디스패치가 수치에 섞였을 것이다

## 막힌 것 / 미완료

- **노드 번호 자동 배정** — 비어 있는 번호를 찾아 선점하는 절차. 오토스케일링에 필요하지만
  "몇 번까지" 라는 상한을 받아야 하고 재시작마다 번호가 바뀌는 대가가 있다. **필요해질 때**
- **대량 접속 해제 구간 미측정** — 1만 개가 한꺼번에 반납되면 버킷을 넘긴다.
  기전은 확인됐지만 **크기는 모른다**
- **별도 OS 프로세스 · 실제 네트워크 분단 · TCP 전송 위의 다중 노드** — 여전히 미검증
- `FragmentAssembler` 조건부 할당 미측정
- Redis Cluster 미지원(Phase 13 잔여)

## 다음

1. **Phase 16 — 대체 전송**(`stateless-web` 참조 프로필이 완성되는 지점).
   "두 프로필이 같은 핸들러로 동작" 이 프로덕션 수준으로 증명되는 자리다
2. Redis Cluster 미지원 해소 — 클러스터 축이 완성됐으므로 `CONSISTENCY.md` 4절의 방안 셋 중 선택
3. 레거시 자격증명 폐기 여부 확인(여러 세션째 대기)
