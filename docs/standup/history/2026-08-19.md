# 2026-08-19 — ⭐⭐ 전수 감사 + P0/P1·설계 결정 전량 반영 (0.2.0)

> 세션은 2026-08-18 에 시작해 자정을 넘겼다. 감사 문서 날짜는 2026-08-18 이다.

## 한 일

- **전수 감사** (`e22b4cc`) — Server 30개 어셈블리 + Bench/Samples + 빌드 인프라(~35k LOC)를
  8개 영역으로 나눠 병렬 정밀 감사(에이전트 8기, 전 소스 정독). 산출:
  `docs/audit/2026-08-18/` 9종(00-summary 종합 실행 계획 + 영역별 8종). 발견 ~60건을
  P0~P3 로 분류, 각각 위치·현재 구현·문제·대안·1.0 전 필수 여부·난이도 기록.
  결론: 코드베이스는 1.0 근접 품질 — "구식 패턴" 부류는 없고, 문제는 원칙의 미세한
  이탈과 동결 전에만 싸게 고칠 수 있는 계약 결함들
- **P0 4건 수정** (`9e8451c`, 전부 회귀 테스트 동반):
  1. TickLoop `MaxCatchUpTicks=0` 에서 정시 루프가 틱 절반 유실(음수 밀림의 정수 절단 +1)
  2. TimerWheel 노드 풀 Treiber 스택 pop ABA(활성 노드 풀 재진입) → `ConcurrentQueue`
  3. Consul BuildView — 비정상 카탈로그(범위 초과·번호/이름 중복·빈 ID) 하나가 멤버십
     루프를 무로그 영구 정지. 사전 검증 + 충돌 등록 전부 제외 + RunAsync catch-all +
     실 Consul 테스트 2종(2026-08-12 보안 검토의 열린 버그를 종결)
  4. `DispatchStatus.Handled=0` → `None=0` 센티넬 재번호(기본값=성공 위장 제거)
- **P1 10건 수정** — TLS `HandshakeTimeout`(기본 10초, slowloris/T-16),
  `ICircuitBreaker.ReleaseProbe()`(취소=성공 오염 제거), Consul HttpClient Timeout 정합
  (WaitTime×17/16+10s), 헬스 accept 루프 예외 격리(liveness 만 죽는 결함), DataTable
  스냅샷 rowCount/tableCount 검증(OOM 방어), TickLoop 이중 Dispose, nullability 어노테이션
  3곳, ClusterView 배열 방어, 재개 응답 상태 엄격화, ClientBuilder 협상 옵션 값 복사
- **설계 결정 4건** (사용자 확정, 전부 권장안 채택):
  - 노드 0 예약 — `NodeId` 생성자 0 거부 + `IsNone`, 유효 번호 1~1023 (**ADR-0074**)
  - `FrameCodecCapabilities` 표면 — 압축·협상의 죽은 조립을 CompositionGuard 가
    시작 시점 거부 (**ADR-0075**, VersionNegotiationOptions 의 "불가능" 보류 해소)
  - `Room.Disband()` → 멤버 스냅샷 반환 + `TryDisband` out 오버로드(해산 창 수습 경로)
  - VERSIONING.md 모순 해소(Shipped 이동 = 1.0 에 1회) + 미방출 메트릭 상수
    `BackpressureDuration` 제거
- **0.1.0 → 0.2.0 승격** — pack 의 ApiCompat 게이트가 전 파괴적 변경을 정확히 검출
  (게이트 실증). ADR-0069 대로 minor 승격 + 억제 파일 2개(Core 15건·Rooms 1건, 의도와
  1:1 일치 검수)
- **취약점 대응** — audit 게이트가 신규 공표된 SSH.NET 2025.1.0 High
  (GHSA-q939-rpr3-3284, Testcontainers 전이)를 검출 → 2026.0.0 전이 고정
- **게이트 통과** — `eng/build.ps1` 6단계(restore·build·test·pack·audit·AOT publish+실행)
  전부 통과, 경고 0

## 결정

- ADR-0074(노드 0 예약) · ADR-0075(프레이밍 capabilities) — 위 참조
- 운영 결정: 감사 발견 반영을 24h soak **앞**에 둔다(soak 는 최종 빌드를 검증해야 의미)

## 측정

- 없음(성능 변경 없음 — TimerWheel 풀 교체는 타이머당 1회 경로라 벤치 대상 아님)

## 막힌 것 / 미완료

- **감사 D 목록(1.0 전 권장)** 미착수 — ROADMAP Phase 22 신규 항목 참조
- 신규 Consul 회귀 테스트 2종은 로컬 Docker 미실행으로 건너뜀 — **CI 초록 확인 필요**
- 정식 24h soak · 최종 성능 기준선 · Shipped 확정 · 1.0 태그 — 종전과 동일

## 다음

1. **CI 초록 확인**(푸시 후) — 특히 Consul 컨테이너 테스트 2종
2. 감사 D 목록 반영(1.0 전 권장 — 전송 축 비대칭·기본값·운영 하드닝류)
3. 정식 24h soak(상세 로거) → 최종 성능 기준선 공표
4. 1.0 경로(표면 점검 → Shipped 전량 이동 → VersionPrefix 1.0 → 게이트 재확인 → 태그)
