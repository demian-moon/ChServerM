# 2026-08-13 — 부분 soak(11h48m) 통과

## 한 일

- **부분 soak 실행·판정** — 2026-08-12 19:09 에 이 대화와 독립된 프로세스
  (`%USERPROFILE%\chserverm-soak-launch.ps1`, `Start-Process` 로 세션과 분리)로 `SoakTests` 를
  띄워 **11시간 48분**(`CHSM_SOAK_SECONDS≈42,521`, InMemory 전송) 연속 churn. 2026-08-13
  06:58 종료, **`dotnet test` exit 0 · 1/1 통과**.
  - 통과 = 테스트의 두 단언 성립: ① `activeConnections == 0` (수많은 connect→16프레임→
    disconnect 사이클 후 커넥션 슬롯이 0 으로 드레인 — 슬롯 누수 없음) ② `final ≤
    baseline×1.5 + 4MB` (정착 메모리 임계 내 평탄). 2026-08-04 감사 H1(죽은 커넥션 항목이
    목록에 영구 잔류) 부류의 회귀 없음
  - 무인 실행 전 절전 AC/DC Never 확인. 로그 `%USERPROFILE%\chserverm-soak-2026-08-13.log`
  - **한계 — 원시 수치 미포착**: baseline/peak/final KB·cycle 수가 로그에 없다. `dotnet test`
    기본 상세도가 **통과한** 테스트의 `ITestOutputHelper` 출력을 억제하기 때문(로그의 인코딩
    깨짐과는 별개 원인). 판정·기간은 확실하나 정확한 메모리 숫자는 이 실행에서 복구 불가.
    **다음 정식 판은 `--logger "console;verbosity=detailed"` 또는 파일 싱크로 수치를 남긴다**

## 결정

- 없음 (ADR 없음)

## 측정

- 부분 soak 11h48m: 커넥션 슬롯 0 드레인 + 메모리 임계 내 평탄, `dotnet test` exit 0.
  원시 KB 는 미포착(위 한계) — BENCHMARKS.md 에는 수치가 없어 넣지 않음

## 막힌 것 / 미완료

- **정식 24h soak**(`CHSM_SOAK_SECONDS=86400`) — 11.8h 부분 통과로 크게 de-risk 됐으나
  게이트 형식 요건은 완전 24h 판(별도 세션, 수치 남기는 로거 설정)
- 최종 성능 기준선 공표 · Shipped 확정 · 1.0 태그 — 종전과 동일
- (선택) Consul `BuildView` 신뢰성 버그 수정

## 다음

1. 정식 24h soak(수치 남기는 로거 설정) → 완료 시 최종 성능 기준선 공표
2. 1.0 준비 완료 판단 시(순서 고정): 표면 점검 → Shipped 전량 이동 → VersionPrefix 1.0 →
   전 Phase 게이트 최종 재확인 → v1.0 태그
3. (선택) Consul `BuildView` 신뢰성 버그 수정

---

# 2026-08-13 (이어서) — ⭐ 공개 API 문서 발행 (GitHub Pages)

## 한 일

- **DocFX API 문서를 GitHub Pages 로 발행** — 공개 URL **https://demian-moon.github.io/ChServerM/** 라이브.
  - `.github/workflows/docs.yml`(`8fcb8bb`) — `dotnet tool restore` + `docfx build` →
    `upload-pages-artifact` → `deploy-pages`. main 푸시 **경로 필터**(Server/** · docs/docfx/**)
    + 수동 dispatch. FlatSharp net9 대비 `DOTNET_ROLL_FORWARD=LatestMajor`
  - **Pages 활성화** — `gh api -X POST .../pages -f build_type=workflow` 로 직접(소유자
    demian-moon 인증, `repo` 스코프). UI 없이 Source=GitHub Actions 설정
  - **발견·수정한 결함**(`d4de9c6`) — 상단 내비 "API 레퍼런스"(`href: api/`)가 404. `docfx.json`
    은 `api/index.md` 랜딩을 기대하는데 파일이 없었다(타입 페이지 336개는 정상). authored 랜딩
    작성 + `git add -f`(‥/api/ 는 .gitignore 대상, .gitignore 는 CP949 라 미수정 — 이 파일만 예외)
  - **소비자 노출**(`59e2d2f`) — README 상단에 API 레퍼런스 링크, `Server/Directory.Build.props`
    의 `PackageProjectUrl` 을 저장소 → 문서 사이트로(nuget "Project website" 가 문서로,
    소스는 RepositoryUrl 유지). 메타는 다음 발행부터 적용
  - 검증: `/`·`/api/`·`/api/…MessageId.html` 전부 HTTP 200(내비 404 해소 확인)

## 결정

- 없음 (ADR 없음 — 문서 호스팅은 아키텍처 결정이 아니다. 발행 트리거는 "경로 필터 main 푸시" 채택)

## 측정

- 없음

## 막힌 것 / 미완료

- 무해한 경고: 워크플로에 Node.js 20 deprecation(actions/cache·deploy-pages 등) — 실행 영향
  없음, dependabot(GitHub Actions 월간)이 승급 예정
- 나머지는 종전과 동일(정식 24h soak · 최종 기준선 · 1.0 경로)

## 다음

1. 정식 24h soak → 최종 성능 기준선 공표
2. 1.0 경로(Shipped 확정 → v1.0 태그)
3. (선택) Consul `BuildView` 신뢰성 버그 · 커스텀 도메인
