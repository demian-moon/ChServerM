// 4자 직렬화 벤치마크 전용 메시지 — 네 포맷이 같은 필드 구성을 갖는다.
syntax = "proto3";

option csharp_namespace = "ChServerM.Bench.Serialization";

message BenchProtoMessage {
  string sender = 1;
  string text = 2;
  int64 timestamp = 3;
}
